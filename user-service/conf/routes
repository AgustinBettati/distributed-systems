# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Default path will just redirect to the computer list

GET        /api/client                     controllers.ClientController.getAllClients
GET        /api/client/:id                 controllers.ClientController.getClientById(id: Integer)

GET        /api/client/:id/products        controllers.ClientController.getClientProducts(id: java.util.UUID)


